# Lingo Memory

- **Whitelist:** Added `EBITDA, COGS, CFADS, IRR, CAPEX, OPEX, GST, Corality, OneLine, DSCR`.
- **Jargon:** Recognized `Offsheet` as common financial modeling jargon, but noted that standardizing to `Off-sheet` or `Off sheet` might be preferred depending on client standards.
- **Terminology:** Majority usage in this workbook uses "Assumptions" for input sheets, though the tab is named "Inputs".
